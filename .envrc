# Fallback if the default asdf-erlang plugin compiles from source (non-macOS or manual plugin).
export KERL_CONFIGURE_OPTIONS="${KERL_CONFIGURE_OPTIONS:-} --without-javac --with-ssl=$(brew --prefix openssl@3)"
